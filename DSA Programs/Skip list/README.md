Write a program to implement a searching algorithm on a skip list having two layers.

Assume that the linked list is already sorted. Write your observations on the time complexity of 
the algorithm.